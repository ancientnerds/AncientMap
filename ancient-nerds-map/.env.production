# Frontend Environment Variables (Vite) - PRODUCTION
# These are exposed to the browser - only put public keys here

VITE_API_BASE_URL=/api
VITE_UPLOAD_URL=/upload

# Mapbox (public token for map tiles)
# Get your free token at: https://account.mapbox.com/access-tokens/
VITE_MAPBOX_ACCESS_TOKEN=<your-mapbox-public-token>

# Cloudflare Turnstile (public site key)
VITE_TURNSTILE_SITE_KEY=<your-turnstile-site-key>
