-- Generated by scripts/remediation/gallery_audit/chunk_writer.py - do not edit by hand.
-- plan sha256 39beb0c8a043cde42f1ea1f37365c3d89e328e64c60eb987c020e0e7cbb7c83f
-- The reversal of lane 'name-key-lyra', chunk 001: 11 row(s) over 6 site(s);
-- scope source_id = 'lyra'; run stamp 'name-key-lyra-2026-09-26-001-rollback'; journal test id 'P6/name-key-lyra'.
-- Every row goes through apply_remediation_change() (migrations 0017/0018/0022): the conditional
-- UPDATE and its journal row are one statement. This file holds no DELETE and no UPDATE.
\set ON_ERROR_STOP on
BEGIN;

CREATE TEMP TABLE _img_plan (
    seq         INTEGER PRIMARY KEY,
    table_name  TEXT NOT NULL,
    column_name TEXT NOT NULL,
    row_key     TEXT NOT NULL,
    site_id     UUID NOT NULL,
    old_value   TEXT,
    new_value   TEXT,
    change_key  TEXT NOT NULL,
    reason      TEXT NOT NULL,
    evidence    JSONB NOT NULL
) ON COMMIT DROP;

INSERT INTO _img_plan (seq, table_name, column_name, row_key, site_id, old_value, new_value, change_key, reason, evidence) VALUES
    (1, 'unified_site_names', 'name_normalized', '3617031', '624a1828-419b-46e4-93d0-ef7b4981aa29'::uuid, 'ヤップ島', 'ヤッフ島', 'name-key-lyra-rollback:unified_site_names.name_normalized:3617031', 'rollback of name-key-lyra chunk 001: unified_site_names.name_normalized on 3617031 returned to ''ヤップ島'' -> ''ヤッフ島''', '[{"quote": "name-key-lyra:unified_site_names.name_normalized:3617031: ''ヤッフ島'' -> ''ヤップ島''", "source": "remediation_change_log (the row this undoes)", "url": "remediation_change_log.run_stamp=name-key-lyra-2026-09-26-001"}]'::jsonb),
    (2, 'unified_site_names', 'name_normalized', '3617034', '624a1828-419b-46e4-93d0-ef7b4981aa29'::uuid, '야프 제도', '야프 제도', 'name-key-lyra-rollback:unified_site_names.name_normalized:3617034', 'rollback of name-key-lyra chunk 001: unified_site_names.name_normalized on 3617034 returned to ''야프 제도'' -> ''야프 제도''', '[{"quote": "name-key-lyra:unified_site_names.name_normalized:3617034: ''야프 제도'' -> ''야프 제도''", "source": "remediation_change_log (the row this undoes)", "url": "remediation_change_log.run_stamp=name-key-lyra-2026-09-26-001"}]'::jsonb),
    (3, 'unified_site_names', 'name_normalized', '3617035', '624a1828-419b-46e4-93d0-ef7b4981aa29'::uuid, '야프섬', '야프섬', 'name-key-lyra-rollback:unified_site_names.name_normalized:3617035', 'rollback of name-key-lyra chunk 001: unified_site_names.name_normalized on 3617035 returned to ''야프섬'' -> ''야프섬''', '[{"quote": "name-key-lyra:unified_site_names.name_normalized:3617035: ''야프섬'' -> ''야프섬''", "source": "remediation_change_log (the row this undoes)", "url": "remediation_change_log.run_stamp=name-key-lyra-2026-09-26-001"}]'::jsonb),
    (4, 'unified_site_names', 'name_normalized', '3617036', '624a1828-419b-46e4-93d0-ef7b4981aa29'::uuid, '야프제도', '야프제도', 'name-key-lyra-rollback:unified_site_names.name_normalized:3617036', 'rollback of name-key-lyra chunk 001: unified_site_names.name_normalized on 3617036 returned to ''야프제도'' -> ''야프제도''', '[{"quote": "name-key-lyra:unified_site_names.name_normalized:3617036: ''야프제도'' -> ''야프제도''", "source": "remediation_change_log (the row this undoes)", "url": "remediation_change_log.run_stamp=name-key-lyra-2026-09-26-001"}]'::jsonb),
    (5, 'unified_site_names', 'name_normalized', '3616936', '6eb8b50f-b9dc-4069-8135-efeb491b9e1b'::uuid, 'ドッガーランド', 'トッカーラント', 'name-key-lyra-rollback:unified_site_names.name_normalized:3616936', 'rollback of name-key-lyra chunk 001: unified_site_names.name_normalized on 3616936 returned to ''ドッガーランド'' -> ''トッカーラント''', '[{"quote": "name-key-lyra:unified_site_names.name_normalized:3616936: ''トッカーラント'' -> ''ドッガーランド''", "source": "remediation_change_log (the row this undoes)", "url": "remediation_change_log.run_stamp=name-key-lyra-2026-09-26-001"}]'::jsonb),
    (6, 'unified_site_names', 'name_normalized', '3616938', '6eb8b50f-b9dc-4069-8135-efeb491b9e1b'::uuid, '도거랜드', '도거랜드', 'name-key-lyra-rollback:unified_site_names.name_normalized:3616938', 'rollback of name-key-lyra chunk 001: unified_site_names.name_normalized on 3616938 returned to ''도거랜드'' -> ''도거랜드''', '[{"quote": "name-key-lyra:unified_site_names.name_normalized:3616938: ''도거랜드'' -> ''도거랜드''", "source": "remediation_change_log (the row this undoes)", "url": "remediation_change_log.run_stamp=name-key-lyra-2026-09-26-001"}]'::jsonb),
    (7, 'unified_site_names', 'name_normalized', '3616643', '848cbe6d-136e-4c15-819c-b017b290a909'::uuid, '북센티널섬', '북센티널섬', 'name-key-lyra-rollback:unified_site_names.name_normalized:3616643', 'rollback of name-key-lyra chunk 001: unified_site_names.name_normalized on 3616643 returned to ''북센티널섬'' -> ''북센티널섬''', '[{"quote": "name-key-lyra:unified_site_names.name_normalized:3616643: ''북센티널섬'' -> ''북센티널섬''", "source": "remediation_change_log (the row this undoes)", "url": "remediation_change_log.run_stamp=name-key-lyra-2026-09-26-001"}]'::jsonb),
    (8, 'unified_site_names', 'name_normalized', '3617346', '85dbd052-9423-4404-a059-3f69cb7bb82d'::uuid, 'ループクンド湖', 'ルーフクント湖', 'name-key-lyra-rollback:unified_site_names.name_normalized:3617346', 'rollback of name-key-lyra chunk 001: unified_site_names.name_normalized on 3617346 returned to ''ループクンド湖'' -> ''ルーフクント湖''', '[{"quote": "name-key-lyra:unified_site_names.name_normalized:3617346: ''ルーフクント湖'' -> ''ループクンド湖''", "source": "remediation_change_log (the row this undoes)", "url": "remediation_change_log.run_stamp=name-key-lyra-2026-09-26-001"}]'::jsonb),
    (9, 'unified_site_names', 'name_normalized', '3617283', 'ccccdfe7-8288-4451-a222-07facc1bfe50'::uuid, 'cerutti mastodon (cm) site', 'cerutti mastodon  site', 'name-key-lyra-rollback:unified_site_names.name_normalized:3617283', 'rollback of name-key-lyra chunk 001: unified_site_names.name_normalized on 3617283 returned to ''cerutti mastodon (cm) site'' -> ''cerutti mastodon  site''', '[{"quote": "name-key-lyra:unified_site_names.name_normalized:3617283: ''cerutti mastodon  site'' -> ''cerutti mastodon (cm) site''", "source": "remediation_change_log (the row this undoes)", "url": "remediation_change_log.run_stamp=name-key-lyra-2026-09-26-001"}]'::jsonb),
    (10, 'unified_site_names', 'name_normalized', '3617349', 'dab28cc0-615f-4779-9fef-882586a6ab6f'::uuid, 'チャーンウッドの森', 'チャーンウットの森', 'name-key-lyra-rollback:unified_site_names.name_normalized:3617349', 'rollback of name-key-lyra chunk 001: unified_site_names.name_normalized on 3617349 returned to ''チャーンウッドの森'' -> ''チャーンウットの森''', '[{"quote": "name-key-lyra:unified_site_names.name_normalized:3617349: ''チャーンウットの森'' -> ''チャーンウッドの森''", "source": "remediation_change_log (the row this undoes)", "url": "remediation_change_log.run_stamp=name-key-lyra-2026-09-26-001"}]'::jsonb),
    (11, 'unified_site_names', 'name_normalized', '3617350', 'dab28cc0-615f-4779-9fef-882586a6ab6f'::uuid, 'チャーンウッドフォレスト', 'チャーンウットフォレスト', 'name-key-lyra-rollback:unified_site_names.name_normalized:3617350', 'rollback of name-key-lyra chunk 001: unified_site_names.name_normalized on 3617350 returned to ''チャーンウッドフォレスト'' -> ''チャーンウットフォレスト''', '[{"quote": "name-key-lyra:unified_site_names.name_normalized:3617350: ''チャーンウットフォレスト'' -> ''チャーンウッドフォレスト''", "source": "remediation_change_log (the row this undoes)", "url": "remediation_change_log.run_stamp=name-key-lyra-2026-09-26-001"}]'::jsonb);

CREATE TEMP TABLE _img_may_empty (site_id UUID PRIMARY KEY) ON COMMIT DROP;
-- no site of this chunk may lose its last live image

CREATE TEMP TABLE _img_live_before ON COMMIT DROP AS
  SELECT DISTINCT w.site_id FROM wiki_images w
   WHERE w.site_id IN (SELECT site_id FROM _img_plan) AND w.is_excluded IS NOT TRUE;

DO $$
DECLARE
    bad      integer;
    moved    integer := 0;
    expected integer := 11;
    r        RECORD;
BEGIN
    -- guard 1: every planned site is a lyra site (the source is a RAISE argument, never
    -- part of the quoted message)
    SELECT count(*) INTO bad FROM (SELECT DISTINCT site_id FROM _img_plan) p
      LEFT JOIN unified_sites u ON u.id = p.site_id
     WHERE u.id IS NULL OR u.source_id <> 'lyra';
    IF bad > 0 THEN
        RAISE EXCEPTION 'lyra alias key recompute rollback chunk 001: % planned site(s) are not % sites', bad, 'lyra';
    END IF;

    -- guard 2: every planned image row exists and lives on the site the plan names
    SELECT count(*) INTO bad FROM _img_plan p
      LEFT JOIN wiki_images w ON w.id = CASE WHEN p.table_name = 'wiki_images' THEN p.row_key::integer END
     WHERE p.table_name = 'wiki_images' AND (w.id IS NULL OR w.site_id IS DISTINCT FROM p.site_id);
    IF bad > 0 THEN
        RAISE EXCEPTION 'lyra alias key recompute rollback chunk 001: % planned image row(s) do not live on the site the plan names', bad;
    END IF;

    -- guard 2b: every planned name row exists and belongs to the site the plan names
    SELECT count(*) INTO bad FROM _img_plan p
      LEFT JOIN unified_site_names n ON n.id = CASE WHEN p.table_name = 'unified_site_names' THEN p.row_key::integer END
     WHERE p.table_name = 'unified_site_names' AND (n.id IS NULL OR n.site_id IS DISTINCT FROM p.site_id);
    IF bad > 0 THEN
        RAISE EXCEPTION 'lyra alias key recompute rollback chunk 001: % planned name row(s) do not belong to the site the plan names', bad;
    END IF;

    -- guard 3: every planned row still holds the planned old value (NULL-safe)

    SELECT count(*) INTO bad FROM _img_plan p JOIN unified_site_names t ON t.id = CASE WHEN p.table_name = 'unified_site_names' THEN p.row_key::integer END
     WHERE p.table_name = 'unified_site_names' AND p.column_name = 'name_normalized'
       AND t.name_normalized::text IS DISTINCT FROM p.old_value;
    IF bad > 0 THEN
        RAISE EXCEPTION 'lyra alias key recompute rollback chunk 001: % planned name_normalized row(s) no longer hold the planned old value', bad;
    END IF;

    -- the only writer
    FOR r IN SELECT * FROM _img_plan ORDER BY seq LOOP
        moved := moved + apply_remediation_change(
            r.table_name, r.column_name, 'id', r.row_key, r.old_value, r.new_value,
            'P6/name-key-lyra', 'name-key-lyra-2026-09-26-001-rollback', r.change_key, 'authoritative', r.evidence, r.site_id);
    END LOOP;
    IF moved <> expected THEN
        RAISE EXCEPTION 'lyra alias key recompute rollback chunk 001: % row(s) changed, % planned', moved, expected;
    END IF;

    -- invariant 1: every planned row now holds its new value

    SELECT count(*) INTO bad FROM _img_plan p JOIN unified_site_names t ON t.id = CASE WHEN p.table_name = 'unified_site_names' THEN p.row_key::integer END
     WHERE p.table_name = 'unified_site_names' AND p.column_name = 'name_normalized'
       AND t.name_normalized::text IS DISTINCT FROM p.new_value;
    IF bad > 0 THEN
        RAISE EXCEPTION 'lyra alias key recompute rollback chunk 001: % planned name_normalized row(s) do not hold the new value', bad;
    END IF;

    -- invariant 2: plan -> journal, row for row, with the planned values and keys
    SELECT count(*) INTO bad FROM _img_plan p
      LEFT JOIN remediation_change_log l
        ON l.run_stamp = 'name-key-lyra-2026-09-26-001-rollback' AND l.table_name = p.table_name
       AND l.column_name = p.column_name AND l.row_pk = p.row_key
     WHERE l.id IS NULL OR l.old_value IS DISTINCT FROM p.old_value
        OR l.new_value IS DISTINCT FROM p.new_value OR l.change_key IS DISTINCT FROM p.change_key;
    IF bad > 0 THEN
        RAISE EXCEPTION 'lyra alias key recompute rollback chunk 001: % planned row(s) disagree with the journal', bad;
    END IF;

    -- invariant 3: journal -> plan: this run stamp journalled the planned rows and nothing else
    SELECT count(*) INTO bad FROM remediation_change_log l
     WHERE l.run_stamp = 'name-key-lyra-2026-09-26-001-rollback'
       AND NOT EXISTS (SELECT 1 FROM _img_plan p
                        WHERE p.table_name = l.table_name AND p.column_name = l.column_name
                          AND p.row_key = l.row_pk);
    IF bad > 0 OR (SELECT count(*) FROM remediation_change_log WHERE run_stamp = 'name-key-lyra-2026-09-26-001-rollback') <> expected THEN
        RAISE EXCEPTION 'lyra alias key recompute rollback chunk 001: the journal of this run stamp is not the plan (% unplanned row(s))', bad;
    END IF;

    -- invariant 4: at most one hero per touched site
    -- the invariant the repair must leave behind: at most one hero per touched site
    -- (DISTINCT: _img_plan holds several rows per site, and a plain join would count each
    --  hero row twice - the check would then fail on a correct repair)
    SELECT count(*) INTO bad FROM (
        SELECT s.site_id FROM (SELECT DISTINCT site_id FROM _img_plan) s
          JOIN wiki_images w ON w.site_id = s.site_id
         GROUP BY s.site_id HAVING count(*) FILTER (WHERE w.is_hero) > 1) x;
    IF bad > 0 THEN
        RAISE EXCEPTION 'lyra alias key recompute rollback chunk 001: % touched site(s) end with more than one hero', bad;
    END IF;

    -- invariant 5: no hero on an excluded row of a touched site
    SELECT count(*) INTO bad FROM wiki_images w
     WHERE w.site_id IN (SELECT site_id FROM _img_plan) AND w.is_hero AND w.is_excluded IS TRUE;
    IF bad > 0 THEN
        RAISE EXCEPTION 'lyra alias key recompute rollback chunk 001: % hero row(s) are excluded', bad;
    END IF;

    -- invariant 6: a touched site that had a live image still has one, unless the chunk names it
    SELECT count(*) INTO bad FROM _img_live_before b
     WHERE NOT EXISTS (SELECT 1 FROM wiki_images w
                        WHERE w.site_id = b.site_id AND w.is_excluded IS NOT TRUE)
       AND b.site_id NOT IN (SELECT site_id FROM _img_may_empty);
    IF bad > 0 THEN
        RAISE EXCEPTION 'lyra alias key recompute rollback chunk 001: % site(s) lost their last live image', bad;
    END IF;

    -- invariant 7: every image kind of a touched row is in migration 0019's vocabulary
    SELECT count(*) INTO bad FROM wiki_images w
     WHERE w.site_id IN (SELECT site_id FROM _img_plan)
       AND w.image_kind IS NOT NULL AND w.image_kind NOT IN ('artifact', 'map_or_document', 'other', 'painting_or_artwork', 'people', 'site_photo', 'unknown');
    IF bad > 0 THEN
        RAISE EXCEPTION 'lyra alias key recompute rollback chunk 001: % image kind(s) outside the vocabulary', bad;
    END IF;
END $$;

SELECT 'journal rows for this run', count(*)::text FROM remediation_change_log WHERE run_stamp = 'name-key-lyra-2026-09-26-001-rollback';
SELECT 'planned rows', count(*)::text FROM _img_plan;
COMMIT;
