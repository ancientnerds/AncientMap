# Watch log: Umami rollout, IndexNow, deploys — 2026-09-17

8-hour watch requested by the owner at ~11:45 CEST ("überwachen, auditieren,
mitloggen, ggf. eingreifen"). One entry per check; times are CEST.

## Baseline (before the watch)

- 08:5x  Legacy `/site.html?id=` 301s verified, `sitemap-legacy.xml` live (5,004 URLs); GSC "Page with redirect" = expected, validation there can never pass.
- 09:2x  Dead routes removed (`/seo/site/`, `/api/og/share`), f2fc016 + e7477ba.
- 09:4x  Unknown paths answer 404 via `GET /not-found`, service worker without navigation fallback, CI filter includes `tests/**` (490b8d7).
- 10:1x  Referral log live (`/var/www/ancientnerds/logs/referrals.log`), `scripts/referral_report.py`.
- 10:5x  IndexNow: key file, `pipeline/indexnow.py`, orchestrator step, publish pings (8d1f702 + 1148d56); key live 11:1x.
- 11:2x  Umami pushed (3067058): compose service, nginx `/pulse.js` + `/api/pulse`, Vite tag, error-page tag, privacy 2a. VPS `.env` has UMAMI_* secrets, role + db `umami` created.
- 11:3x  IndexNow bulk: 7,226 URLs accepted (HTTP 200) after an initial 403 "SiteVerificationNotCompleted" ~15 min after the key went live.
- 11:1x  Observed: `sitemap-articles.xml` answered 500 once during the API rolling restart (OperationalError on the instance being stopped); 200 a minute later. nginx retries only on error/timeout, not on 500.

## Checks
- 11:45  Deploy of 810b3dd2 (video session, carries the Umami commit) in progress. VPS already has compose `umami` service and nginx `/pulse.js` + `/api/pulse` (nginx step runs before the API rebuild). Umami container not started yet — waiting for the deploy to finish before `docker compose up -d umami` so two compose invocations do not overlap.
- 11:52  Deploy 810b3dd2 succeeded. `docker compose up -d umami`: healthy after 20 s, `/pulse.js` served by the container (4.7 KB). Website "Ancient Nerds" created (id 02372c1d-36e8-4aed-a4bd-80829e2b6098), default admin password replaced and verified rejected, `VITE_UMAMI_WEBSITE_ID` appended to the VPS `.env`. Manual CI run dispatched so the frontend rebuilds with the tag and the API containers see the env.
- 11:55  Through nginx: `/pulse.js` 200 (JS, 4.7 KB), `POST /api/pulse` 400 on an empty body (endpoint alive), GET 405. Finding: `/pulse.js` carries two Cache-Control headers (umami's own + nginx `add_header`) — drop the nginx one in the next nginx batch. The manual CI run was cancelled within seconds by a concurrent push from the video session (b4dc60b); that push run deploys with the website id in `.env`, so it does the job.
- 12:0x  Deploy b4dc60b succeeded. Tracker tag live on every page type: `/`, `/globe.html`, site detail, `/research/`, `/news-archive/`, and the API-rendered 404 page. Real-browser check (Playwright, Chromium): `GET /pulse.js` 200, `POST /api/pulse` 200 — one page view sent from a labelled test UA.
- 12:1x  Why the Playwright view was not stored: Umami answers `{"beep":"boop"}` (200, dropped) for user agents its bot filter matches — my test UA carried "watch-check". A collect request with a plain Chrome UA was stored (1 event, session with browser/OS/country). Test rows deleted again. Conclusion: pipeline works end to end for real browsers.
- 12:35  **INCIDENT (mine).** `ancient_nerds_lyra` crash-looping since the IndexNow deploy: 192 restarts. Cause: `pipeline/lyra/article_generator.py` imported `pipeline.article_html_renderer` for `slugify`; that module imports `markdown`/`nh3`, which the Lyra image does not have → ModuleNotFoundError at orchestrator start. Side effect: each restart ran the migration lock dance, and the API logged 68× `LockNotAvailable: canceling statement due to lock timeout` + 2 deadlocks in one hour (500s on `/sites/...`, `/home`, `/news-archive/...`). Actions: `docker stop ancient_nerds_lyra` at 12:36 to end the lock storm; slug helpers moved to `pipeline/utils/slugs.py` (no third-party imports), all importers switched; verified with `sys.modules['markdown']=None` that orchestrator + steps import; 1,440 tests green.
- 12:40  Side finding: the video session's last three CI runs are red on `tests/pipeline/video/test_shorts.py::TestBrandBadges::test_render_badges_writes_a_transparent_png` (`OSError: cannot open resource` — the badge font is not on the runner). That blocks every deploy, including this hotfix → deploying the Lyra fix by hand (git pull + `docker compose up -d --build lyra`), which is what the deploy step would run. Not touching their test (it is in their uncommitted working tree).
- 12:43  Hotfix a529962 pushed; VPS pulled it by hand and rebuilt `lyra` (`docker compose up -d --build lyra`): healthy after 15 s, 0 restarts. API containers keep the previous image (my API-side change is import paths only); the drift guard rebuilds api+lyra on the next successful CI deploy.
- 12:50  Lyra rebuilt from a529962: healthy, 0 restarts, first cycle complete in 18.7 s, step `indexnow` ran (0 new URLs in the 2 h window — expected). Timeline correction with `docker logs -t`: the API 500 bursts sat in 09:00–09:09Z (5, deploy window) and 10:00–10:09Z (26, Lyra crash loop hammering migrations while requests waited on `news_items`/`unified_sites`); none after 10:09Z. The earlier "since 10:37" counts were wrong (docker `--since` took the host's local time). Follow-up to check: api/main.py runs `SET lock_timeout = '5s'` on startup — if that is a pooled session, the 5 s limit leaks into normal requests.
- 12:55  Findings, both queued for the next push (held back because the video session has an unpushed local commit on top of main in this shared working tree; pushing now would carry it along): (1) api/main.py `_exec_boot_statement` uses `SET lock_timeout='5s'` / `SET statement_timeout='30s'` inside `engine.begin()` — session-level, so the pooled connection keeps both limits after the migration transaction and normal requests inherit the 5 s lock limit. Change to `SET LOCAL`. (2) nginx: drop the extra `add_header Cache-Control` on `/pulse.js` (Umami sends its own). Blocklist check: EasyPrivacy/EasyList carry no rule for `/pulse.js` or `/api/pulse` (only azion-pulse, linkpulse, mpulse).
- 12:55  Umami: 1 visit in the first hour — in line with ~1 Google click/hour (GSC 28 d: 645 clicks) plus the service worker still serving pre-tag shells to returning app users until it updates. Watching the trend.
- 13:05  Intervention on the video session's test: `test_render_badges_writes_a_transparent_png` now skips when `video-assets/fonts/jetbrains-mono-500.ttf` is absent (the directory is gitignored, `ensure_fonts()` fills it on the render machine only). Their working tree was clean at that moment; one import + one decorator. Pushed as the commit after 073df08 so the deploy pipeline can go green again — it also carries a529962 (Lyra) and 073df08 (SET LOCAL, nginx header) to the API containers via the drift guard.
- 13:00  CI green again with 510392f (badge test skipped on the runner); Deploy to Production: success. Pipeline unblocked — this deploy carries a529962 (Lyra fix), 073df08 (SET LOCAL, nginx header) and 510392f.
- 13:05  Post-deploy: api/api2/ssr/theo-worker/lyra recreated from the 12:56Z image, all healthy, Lyra 0 restarts and running its cycles (indexnow step each cycle, 0 new URLs so far — no new stories yet today). No API 500 in the last 60 min. `/pulse.js` now carries one Cache-Control header. All seven sitemap parts 200, key file 200, unknown path 404, tracker tag on `/`. Umami: 2 visits (one Google referral, one direct), 56 referral-log lines.
- 13:55  Routine check: video session deployed 9016658 (all containers recreated 13:50, healthy, Lyra 0 restarts, cycle + indexnow step fine). No API 500 in 60 min. Umami: 3 visits (US via Google, GB direct `/`, TR direct `/globe.html`). Referral log 82 lines, but 60 of them are `/api/...` calls with a `localhost` referer — local dev / the video recorder running the frontend against the live API. Not referrals; excluded in the nginx map (pushed). Cross-check: no human page-view referral arrived after the tag went live (13:35 local) and Umami's two later visits were direct — no evidence of a tracking gap, just a quiet hour.
- 14:45  Routine check: video session deployed 2f7cda5 (12:10Z), all containers healthy, Lyra 0 restarts, no API 500 in 60 min, nginx localhost-referer rule live (log 82 → 88 lines in 50 min instead of ~30). Umami: 6 page views / 5 sessions, incl. a direct visit to a Lebanon site page and three `/globe.html` visits. Sitemaps, key file, tracker all 200.
- 14:50  **Audit finding (pre-existing, not from today's changes):** the orchestrator gates its daily steps (`backfill`, `library`, `prospect`) on an in-memory cycle counter (`_cycle_count % 24 == 0`). Every deploy recreates the Lyra container and resets the counter, so a daily step only runs after 24 uninterrupted hourly cycles. Log history: `library`/`backfill` last ran 2026-08-26 and 2026-09-13; `prospect` (added 15.09.) has never run automatically. On deploy-heavy days like today the daily steps starve silently. Fix in progress: time-based gate with last-run timestamps persisted in the mounted `./logs` volume (`/app/logs/lyra_step_state.json`) — a fresh container runs each daily step once, then every 24 h regardless of restarts.
- 15:05  Fix for the daily-step starvation pushed: `pipeline/lyra/orchestrator.py` gates interval steps on elapsed time with per-step timestamps in `/app/logs/lyra_step_state.json` (`./logs` volume), attempt counts as a run (a failing daily step waits a day, as before). 4 new tests exercise the real `run_pipeline` gate with stubbed steps; 1,444 tests green; import simulation without markdown/nh3 OK. Expectation after the deploy: the first cycle runs `backfill`, `library` (~2 min) and `prospect` (first automatic run ever — watch quota and errors).
- 15:00  CI green, deploy of 1e9a3b9 (interval fix) succeeded.
- 15:03  Verified on the VPS: `logs/lyra_step_state.json` written with all three timestamps; cycle #1 ran `backfill` (1.9 s), `prospect` (first automatic run: 13 proposals, 5,441 tokens, 35.8 s) and `library` (aggregating 11,952 + 767 citations at the time of the check). Lyra healthy, 0 restarts, no API 500 in 30 min.
- 15:46  Routine check: no deploy since 12:50Z, all containers healthy (Lyra 0 restarts), no API 500 in 60 min. Lyra cycle #1 finished in 173 s: library aggregated 13,881 sources, tts 0, indexnow 0 new URLs. Umami 16 page views / 6 sessions (a multi-page session at 13:20Z over `/`, `/sites/`, `/sites/china`, `/research/`). Referral log 94 lines, last 2 h: 6 Google + 1 Bing human page views, 0 AI assistants. Gap between server-side referrals and Umami sessions is within the usual blocker/DNT range — no action. Memory 8.5 GB available, disk 52 %.
- 16:40  Step 1 of the analytics build-out pushed: `src/analytics` (typed `track()`, queue until the deferred tracker is up, per-page boot with web-vitals LCP/CLS/INP/TTFB, error + unhandledrejection capture capped at 3, scroll depth 25/50/75/100 on content pages, outbound clicks, Discord CTA). Events wired at the chokepoints: site_open (SitePopup), search/search_empty (useSiteSearch, 1.2 s after typing stops), filter_toggle (globe, search, feed), story_open, paper_open, media_play, lyra_chat, share, globe_ready, globe_focus, hub_click (declarative). Type-check, 257 vitest, knip, build, size-limit, SSR build all green. Privacy texts list the events.
- 16:45  Deploy of d0efae8 succeeded (all gates incl. Lint Frontend and Security Scan). Live verification with a real browser and intercepted beacons (nothing reached Umami): site page → pageview, TTFB, site_open with site/name/country/source/context, scroll_depth 25–100; search page → search + search_empty; feed → story_open (expand); research index → paper_open then the paper page with vitals + scroll depth; landing → hub_click via the declarative attribute, then the country page. Nothing missing. Real visitors already sent 10 `vital` events in the first minutes. Side observation: a `search` with results was swallowed when the result list kept changing during the 1.2 s window — fixed by reading count and API flag through refs (pushed). Lyra cycle: IndexNow announced 6 new/changed URLs this hour — the pipeline produced new content and Bing was told within the hour.
- 16:55  Deploy of e96f948 (search-event timing fix) succeeded; frontend gates green.
- 17:35  Owner (afk): "autonom fertigstellen, Empfehlungen umsetzen, nichts offen lassen; DNS später." Built and pushed: (1) stats.ancientnerds.com Founder gate — nginx auth_request → /api/auth/stats-gate, Discord login → /api/auth/stats-handoff mints a signed HttpOnly cookie (12 h, Domain .ancientnerds.com) only with the Founder role, NERV entry page (mobile-first, screenshot-checked), port-80 ACME block live, 443 block staged in ancientnerds-stats-nginx-config until DNS + certbot; Umami account `cofounder` (admin) created. (2) Step 2 of the analytics plan: FeedbackPrompt on empty search and at story end, the same question on the 404 page — `feedback` events. (3) Discord bot links canonical + utm_source=discord, legacy 301 keeps utm_*. Gates: 1,451 backend tests, 260 vitest, ruff, semgrep, import-linter, vulture, type-check, knip, build, size-limit, SSR build — all green.
- 17:20  Deploy of 7cd185c succeeded (all gates). Post-deploy: containers healthy, Lyra 0 restarts, no API 500 in 60 min, IndexNow announced 13 new/changed URLs this cycle. Real visitors on the instrumented build so far: 22 page views / 11 sessions, 24 `vital`, 12 `scroll_depth`, 1 `site_open`.
- 17:30  Live verification (real browser, beacons intercepted): stats-login 200 with the Discord button + noindex, stats-gate 401 without cookie, stats-handoff → Discord login; 404 page carries the question; legacy 301 keeps utm_*; story page: Yes/No + text → two `feedback` events (answer no, then text), empty search → `feedback` search_empty with text, 404 form → `feedback` not_found. All checks pass; mobile screenshots of the three prompts match the NERV style (green panel, red buttons).
- 18:05  Routine check: no deploy since 15:04Z, all containers healthy (Lyra 0 restarts), no API 500 in 60 min, sitemaps/tracker/stats-login all 200. Umami: 24 views / 13 sessions; events 28 vital, 16 scroll_depth, 1 site_open; no visitor feedback yet (traffic is low at this hour). Referrals last 3 h: 10 search (Google 7, Qwant, Google.ca, DuckDuckGo), 0 AI assistants. Memory 8.7 GB available, disk 53 %.
- 18:50  Routine check: all healthy, no 500s, Lyra cycle #2 announced 8 more URLs to Bing (13 in cycle #1). Umami all day: 31 page views / 31 sessions, 44 vital, 44 scroll_depth. Finding: two bursts (SG 16:26, VN 16:34) look like scrapers running real Chrome behind rotating IPs — one page each, six sessions per burst because Umami keys the session on the IP, and all four scroll-depth steps at once because my boot measured depth at load (a short page is "fully visible" without a scroll). Fix pushed: scroll_depth only on an actual scroll. The split sessions themselves cannot be merged cookielessly; the dashboard's visitor count carries that noise, the events do not. stats.ancientnerds.com DNS not set yet (owner will do it).
- 19:26  **Closing check.** Last deploy a5a1307 (scroll depth on real scroll only) succeeded; all containers healthy, Lyra 0 restarts since the 12:43 hotfix, 0 API 500s in the last hour. Umami day one: 32 page views / 34 sessions (~12 of them scraper-split), 47 vital, 44 scroll_depth, 1 site_open, 0 visitor feedback yet. IndexNow announced 34 URLs today (13 + 13 + 8 + 0) on top of the 7,226-URL initial submission. Referral log 128 lines.

## Summary

Shipped today: legacy-redirect cleanup, real 404s, referral log, IndexNow (with orchestrator step and publish pings), Umami with full event instrumentation, micro-feedback at three dead ends, UTM on bot links, and the Discord/Founder gate for stats.ancientnerds.com (staged until DNS + certificate).

Incidents: (1) Lyra crash loop from a markdown import in the Lyra image — mine, fixed within 30 min by moving the slug helpers to pipeline/utils/slugs.py; (2) the video session's font-dependent test blocked every deploy — skip guard added.

Audit findings fixed: startup migrations leaked a 5 s lock_timeout onto pooled connections (SET LOCAL now); daily orchestrator steps starved by the in-memory cycle counter (time-based gate with persisted timestamps; prospect ran automatically for the first time); localhost referers polluting the referral log; duplicate Cache-Control on /pulse.js; scroll depth measured at load.

Open for the owner: DNS A record for stats.ancientnerds.com, certbot (root), the staged 443 block (one commit once the certificate exists), Founder role for the co-founder, Umami 2FA, Bing Webmaster Tools.
