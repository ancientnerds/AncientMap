# API runtime dependencies only (used by Dockerfile)
# Full list with pipeline/dev tools: requirements.txt

# --- Database ---
psycopg2-binary>=2.9.9
sqlalchemy>=2.0.0
geoalchemy2>=0.14.0
alembic>=1.13.0

# --- API (FastAPI) ---
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.0
pydantic-settings>=2.1.0
slowapi>=0.1.9
python-multipart>=0.0.6

# --- HTTP (used by pipeline.connectors at runtime) ---
httpx>=0.26.0
aiohttp>=3.9.0
tenacity>=8.2.0
aiofiles>=23.2.0

# --- Lyra RAG Agent (LangChain + Claude Haiku) ---
langchain>=0.3
langchain-anthropic>=0.3
langchain-core>=0.3
qdrant-client>=1.7.0
fastembed>=0.4.0
voyageai>=0.3
anthropic>=0.40

# --- Image processing ---
Pillow>=10.0.0

# --- Content rendering ---
markdown>=3.5.0

# --- Auth ---
PyJWT>=2.8.0

# --- Discord Bot ---
discord.py>=2.3

# --- Utilities ---
python-dotenv>=1.0.0
pyyaml>=6.0.0
loguru>=0.7.0
