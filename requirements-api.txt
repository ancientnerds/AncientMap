# API runtime dependencies only (used by Dockerfile)
# Full list with pipeline/dev tools: requirements.txt

# --- Database ---
psycopg2-binary>=2.9.9
sqlalchemy>=2.0.0
geoalchemy2>=0.14.0
alembic>=1.13.0

# --- API (FastAPI) ---
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.0
pydantic-settings>=2.1.0
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
slowapi>=0.1.9
python-multipart>=0.0.6

# --- HTTP (used by pipeline.connectors at runtime) ---
httpx>=0.26.0
aiohttp>=3.9.0
tenacity>=8.2.0
aiofiles>=23.2.0

# --- AI / Vector search (lazy-loaded) ---
# NOTE: torch is installed separately with CPU-only index in Dockerfile
qdrant-client>=1.7.0
sentence-transformers>=2.2.2
transformers>=4.40.0

# --- Image processing ---
Pillow>=10.0.0

# --- Utilities ---
python-dotenv>=1.0.0
pyyaml>=6.0.0
loguru>=0.7.0
