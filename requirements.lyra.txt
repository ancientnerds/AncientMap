psycopg2-binary>=2.9.9
sqlalchemy>=2.0.0
geoalchemy2>=0.14.0
pydantic>=2.5.0
pydantic-settings>=2.1.0
python-dotenv>=1.0.0
anthropic>=0.50.0
openai>=1.30.0
youtube-transcript-api>=1.0.0
# Pinned to a dev build: YouTube's SABR rollout broke 720p segment downloads
# for 2026.7.4 (403 on every clip). Naming a .dev0 version also tells pip it
# may install pre-releases for this package (PEP 440).
yt-dlp>=2026.8.17.73947.dev0
Pillow>=10.0.0              # screenshot width check (hi-res backfill)
requests>=2.31.0
httpx>=0.26.0
tenacity>=8.2.0
loguru>=0.7.0

# Cache client for api.cache (shared Redis instead of in-memory fallback)
redis>=5.0.0

# ID3 tagging of generated TTS audio (Art. 50(2) EU AI Act marking)
mutagen>=1.47.0
